package main

import (
	"fmt"
	"math"
	"math/rand"
)
func main() {
	var nickels, dimes, quarters float32 = 0.05, 0.10, 0.25
	total float32 := 
	for math.Abs(20 - total) > 0 {
		value := rand.Float32()
		total += value
	}
	fmt.Println("Test")
}
